{
    "name": "Daws developer container",
    "image": "mcr.microsoft.com/devcontainers/base",
    "features": {
        "ghcr.io/devcontainers/features/docker-in-docker:2": {
            "moby": false
        }
    },
    "postCreateCommand": "curl -L https://nixos.org/nix/install | sh -s -- --no-daemon && . \"$HOME/.nix-profile/etc/profile.d/nix.sh\" && curl -fsSL https://get.jetify.com/devbox | bash -s -- -f && devbox install"
}