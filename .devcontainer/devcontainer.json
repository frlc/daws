{
    "name": "Daws developer container",
    "image": "mcr.microsoft.com/devcontainers/base",
    "features": {
        "ghcr.io/devcontainers/features/docker-in-docker:2": {
            "moby": false
        }
    },
    "postCreateCommand": "sh <(curl -L https://nixos.org/nix/install) --no-daemon && . \"$HOME/.nix-profile/etc/profile.d/nix.sh\" && curl -fsSL https://get.jetify.com/devbox | bash -s -- -f && devbox install"
}